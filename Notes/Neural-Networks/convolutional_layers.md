# Convolutional Layer
type of layer where each neuron looks at a region of input image is called a convolutional layer 
![alt text](<../../Assets/Screenshots/image copy 4.png>)
And if we have multiple convolutional layers in a neural network ​sometimes that's called a convolutional neural network